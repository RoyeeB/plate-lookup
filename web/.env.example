# Error reporting (optional). Leave unset to keep monitoring off — the Sentry
# SDK is then never downloaded. Create a project at sentry.io and paste its DSN.
VITE_SENTRY_DSN=
# Shown as the release in error reports, e.g. the git commit.
VITE_APP_VERSION=
